# Local Agent Instructions
- Always use the emerald green accent (#50C878) for new UI components.
- When creating components like the ROI Calculator, place them in `/src/components`.
- Use Tailwind CSS for styling unless specified otherwise.
- Terminal: You have permission to run `npm start` to verify UI changes.